ROLE: You are an expert web developer and data analyst. Your specialty is mapping raw, scraped website data to a structured JSON configuration.

GOAL: Your task is to analyze the attached files (homepage-screenshot.png, style_map.json, example-client.json) and populate the empty template. The final output must be a single, valid JSON object.

METHODOLOGY: Follow this strategic analysis. The screenshot provides the visual context and is the source of truth for the layout and hierarchy. Use the style_map.json to find the specific data values that correspond to the visual elements.

Step 1: Color Analysis Strategy

Background: First, identify the primary background color of the page from the screenshot. Find this color's value in the style_map.json, typically from the body tag's styles, and assign it to colors.background.

Text: Identify the most common color for paragraph text (<p> tags) in the screenshot. Find its value in the style_map.json and assign it to colors.text.

Primary: This color is for main headings (h1, h2). Identify the most common color for these elements in the screenshot and find its value in the data. Assign this to colors.primary.

Accent: This color is for interactive elements like buttons or standout links. Find a color used for these elements in the screenshot and assign its value to colors.accent.

Fallback Logic for Empty Fields:

If you cannot find a clear value for colors.background-alt but colors.background is defined, generate a new color that is a complementary, slightly lighter or darker shade of the main background.

Do the same for colors.secondary (based on colors.primary) and colors.text-muted (based on colors.text) if they cannot be found directly.

Step 2: Typography Analysis Strategy

Derive the headingFont and headingWeight from the most common font-family and font-weight found in h1 and h2 tags in the style_map.json.

Derive the bodyFont and bodyWeight from the most common font-family and font-weight found in p tags.

Step 3: Content Mapping Strategy

Use the screenshot to identify the different sections of the page. Then, find the corresponding content in the style_map.json to populate the component props.

HeroSection: Visually identify the largest title and its subtitle. Find these strings in the style_map.json and use their content for props.title and props.subtitle.

ServicesGrid: Look for a repeating pattern of a heading followed by one or more paragraphs. Each pattern represents a "service."

Infer the title: Look for a heading that logically introduces these service sections. Use its content for the main props.title. Avoid using a generic title like "Services" if a more descriptive one is available.

Create an object in the props.services array for each distinct section you identify.

Footer: Visually identify the footer section at the bottom of the page.

Find text that serves as a credit or copyright line and use its content for props.copyrightText.

Extract any contact information like phone numbers or emails and place them in props.contactInfo.

Step 4: Output Constraints (CRITICAL)

The final output must be ONLY the fully populated JSON object.

Do not include any explanations, apologies, introductory text, or markdown formatting.

The entire response must be a single, valid JSON object.

If, after following the fallback logic, a field still cannot be populated, leave it as an empty string "" or an empty array [].